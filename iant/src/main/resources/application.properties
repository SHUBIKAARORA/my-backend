spring.application.name=iant

# ========== DB CONNECTION ==========
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DB}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=8080

# ========== JPA CONFIG ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ========== SPRING SECURITY ==========
# This is optional but useful to change default login page if needed
# spring.security.user.name=admin
# spring.security.user.password=admin

# Allow custom login handling
# By default, Spring Security will redirect to /login, but you can control that in your SecurityConfig

